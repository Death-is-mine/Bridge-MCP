# Workflow: Continuous Development Loop

The product supports repeated cycles without forcing the AI or Bridge to become an unrestricted autonomous shell.

```text
inspect
→ plan
→ edit
→ test
→ diff
→ review
→ feedback
→ repeat
```

The user remains able to stop or approve sensitive steps.
