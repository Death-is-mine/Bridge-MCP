# Environment

Expected variables are examples only and must match implementation.

```env
OPENCODE_BASE_URL=http://127.0.0.1:4096
OPENCODE_SERVER_PASSWORD=
BRIDGE_AUTH_SECRET=
GITHUB_TOKEN=
GITHUB_REPO=
```

Never commit secrets.

Provide `.env.example`, never real credentials.
